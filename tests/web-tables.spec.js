import { test } from "@playwright/test";

test.describe("Test Group", () => {
  test("1", async ({ page }) => {
    // Test body
  });

  test("2", async ({ page }) => {
    // Test body
  });

  test("3", async ({ page }) => {
    // Test body
  });
});
